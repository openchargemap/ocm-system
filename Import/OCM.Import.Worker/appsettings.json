{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ImportSettings": {
    "MasterAPIBaseUrl": "https://api-01.openchargemap.io/v3",
    "ImportRunFrequencyMinutes": 1,
    "OCPIProvidersConfigPath": "ocpi-providers.json",
    "EnabledImports": [
      // The worker will fetch DataProvider list from OCM API
      // and automatically schedule imports based on DateLastImported
      // Only providers listed here will be imported (non-OCPI providers)
      // OCPI providers are managed via ocpi-providers.json (IsEnabled flag)
      // Providers are prioritized by oldest DateLastImported (> 24 hours)

      // "afdc.energy.gov", duplicates

      // Retired providers:
      // "chargepoints.dft.gov.uk" - service discontinued
      // "nobil.no" - migrated to other system
    ],
    "GeolocationShapefilePath": "Shapefiles/World",
    "TempFolderPath": "/tmp/",
    "KeyVaultUri": "",
    "KeyVaultTenantId": "",
    "KeyVaultClientId": "",
    "KeyVaultSecret": ""
  }

}
